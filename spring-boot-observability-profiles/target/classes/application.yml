spring:
  application:
    name: spring-boot-observability
  profiles:
    active: prometheus

management:
  endpoints:
    web:
      exposure:
        include: "*"
  observations:
    annotations:
      enabled: true
