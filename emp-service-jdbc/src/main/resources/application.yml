spring:
  application:
    name: emp-service
  datasource:
   url: jdbc:mysql://localhost:3306/springdb
   username: ${DB_USER:root}
   password: ${DB_PASSWORD:P@ssword}
   hikari:
      connection-init-sql: select count(empno) from myemp;
      minimum-idle: 5
      
      

logging:
  level:
    '[org.springframework.jdbc]': debug

management:
  endpoints:
    web:
      exposure:
        include: "*"

    